import { Button } from "react-bootstrap";

const Main = () => {
  return (
    <div>
      <div>Hello there!</div>
      <Button variant="dark" className="uniq__btn">Кнопка</Button>
    </div>
  );
};

export default Main;
